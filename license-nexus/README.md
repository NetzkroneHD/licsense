# Setup

## Start

```sh
docker compose up
```

## Configure

1. Open file `/nexus-data/admin.password`
2. Login with `admin`
3. Open `Settings` -> `Security` -> `Users` -> `Anonymous User`
4. Add `nx-admin` Role to the User
